import styled from 'styled-components';
import { P } from '../../constants/styled';

export const Wrapper = styled.section`
  background-color: #000;
`;

export const Text = styled(P)`
  color: #fff;
  padding: 1rem;
  text-align: center;
`;
